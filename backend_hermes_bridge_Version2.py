# Placeholder Hermes Agent bridge. Replace with actual Hermes Agent SDK usage.
# Example API that the server.py will call to get agent responses.

# from hermes_agent import Agent
# agent = Agent()

def solve(problem: str):
    """
    Call Hermes Agent (or other LLM/agent) and return raw response text.
    Currently this is a placeholder that returns a simple explanation.
    Replace with the real agent call and streaming logic.
    """
    # response = agent.chat(problem)
    # return response
    return "Interior angles of any triangle sum to 180 degrees because..."